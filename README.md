# متجر إلكتروني متكامل 🛍️

متجر إلكتروني حديث وكامل مبني بـ Flask مع واجهة عربية جميلة وسهلة الاستخدام.

## المميزات الرئيسية ✨

- **نظام تسجيل دخول وحسابات موثوق** - تسجيل جديد وتسجيل دخول آمن
- **عرض المنتجات** - عرض جميع المنتجات مع تفاصيل دقيقة
- **البحث والتصفية** - بحث متقدم وتصفية حسب الفئة
- **سلة التسوق** - إدارة منتجات السلة بسهولة
- **نظام الطلبات** - متابعة الطلبات والحالات
- **حسابي الشخصي** - إدارة معلومات الحساب والعناوين
- **قاعدة بيانات SQLite** - تخزين آمن للبيانات
- **تشفير كلمات المرور** - حماية بيانات المستخدمين

## المتطلبات 📋

- Python 3.7+
- Flask 3.0.0
- Werkzeug 3.0.0

## التثبيت والتشغيل 🚀

### 1. تثبيت المكتبات

```bash
pip install -r requirements.txt
```

### 2. تشغيل التطبيق

```bash
python app.py
```

### 3. فتح المتجر

افتح متصفحك وانتقل إلى:
```
http://localhost:5000
```

## الصفحات المتوفرة 📄

| الصفحة | الرابط | الوصف |
|--------|--------|-------|
| تسجيل الدخول | `/login` | دخول المستخدمين |
| إنشاء حساب | `/register` | تسجيل حساب جديد |
| المتجر | `/store` | عرض جميع المنتجات |
| تفاصيل المنتج | `/product/<id>` | عرض تفاصيل منتج واحد |
| السلة | `/cart` | عرض منتجات السلة |
| الدفع | `/checkout` | إكمال عملية الشراء |
| تأكيد الطلب | `/order-confirmation/<id>` | تأكيد الطلب |
| الطلبات | `/orders` | عرض جميع طلباتي |
| حسابي | `/profile` | معلومات الحساب الشخصي |
| تسجيل الخروج | `/logout` | خروج آمن |

## هيكل المشروع 📁

```
python261/
├── app.py                 # الملف الرئيسي
├── requirements.txt       # المكتبات المطلوبة
├── ecommerce.db          # قاعدة البيانات (ينشأ تلقائياً)
└── templates/            # ملفات HTML
    ├── login.html
    ├── register.html
    ├── store.html
    ├── product_detail.html
    ├── cart.html
    ├── checkout.html
    ├── order_confirmation.html
    ├── orders.html
    └── profile.html
```

## المنتجات الافتراضية 📦

التطبيق يأتي مع 12 منتج تجريبي:
- **الكترونيات**: كمبيوتر محمول، هاتف ذكي، سماعات، كاميرا، ساعة ذكية، جهاز لوحي، قارئ كتب
- **الملحقات**: ماوس لاسلكي، لوحة مفاتيح، حامل هاتف، كابل USB-C، محطة شحن

## قاعدة البيانات 🗄️

تتضمن المشروع 5 جداول:

1. **users** - بيانات المستخدمين
2. **products** - المنتجات المتاحة
3. **cart** - عناصر سلة التسوق
4. **orders** - الطلبات المكتملة
5. **order_items** - تفاصيل منتجات الطلبات

## معلومات الأمان 🔒

- كلمات المرور مشفرة باستخدام Werkzeug
- جلسات المستخدم آمنة مع Flask sessions
- رسالة خطأ آمنة لا تكشف معلومات حساسة
- حماية ضد الوصول غير المصرح للصفحات

## حسابات التجربة 👨‍💻

يمكنك إنشاء حسابات جديدة من خلال صفحة التسجيل أو استخدام البيانات الافتراضية:
- اسم المستخدم: test
- كلمة المرور: test123

## التطوير المستقبلي 🔮

الميزات المخطط إضافتها:
- [ ] نظام الدفع الفعلي
- [ ] تقييمات المنتجات والتعليقات
- [ ] قوائم المفضلة
- [ ] عروض وخصومات
- [ ] إدارة المخزون
- [ ] لوحة تحكم للمسؤول

## استكشاف الأخطاء 🐛

### خطأ "ModuleNotFoundError: No module named 'flask'"
```bash
pip install Flask==3.0.0
```

### المتجر لا يعمل على الويب
تأكد من تشغيل الخادم:
```bash
python app.py
```

### مشكلة في قاعدة البيانات
حذف ملف `ecommerce.db` وأعد تشغيل التطبيق لإنشاء قاعدة جديدة

## المراجع والموارد 📚

- [Flask Documentation](https://flask.palletsprojects.com/)
- [Werkzeug Security](https://werkzeug.palletsprojects.com/security/)
- [SQLite Documentation](https://www.sqlite.org/docs.html)

## الترخيص 📝

هذا المشروع مفتوح المصدر ومتاح للاستخدام التعليمي.

## التواصل والدعم 📞

في حالة وجود أي مشاكل أو استفسارات، يرجى التواصل أو فتح issue جديد.

---

**ملاحظة**: هذا المشروع للأغراض التعليمية والتطوير. للاستخدام الإنتاجي، يتطلب تحسينات أمنية إضافية.

## نشر إلى GitHub وRender (خلاصة سريعة)

فيما يلي خطوات سريعة لرفع المشروع إلى GitHub ونشره على Render. يمكنك تنفيذ الأوامر أدناه من PowerShell على جهازك المحلي داخل مجلد المشروع (`c:\Users\abdo\python261`).

1) تهيئة المستودع المحلي ورفع الملفات إلى GitHub

```powershell
git init
git add .
git commit -m "Initial commit: Flask e-commerce"
# إما استخدام أداة gh (GitHub CLI) إذا مثبتة:
# gh repo create <your-username>/<repo-name> --public --source=. --remote=origin --push
# أو إنشاء repo يدوياً على GitHub ثم ربطه ودفعه:
# git remote add origin https://github.com/<USERNAME>/<REPO>.git
# git branch -M main
# git push -u origin main
```

2) نشر على Render

- سجّل الدخول إلى https://render.com واختر "New" → "Web Service".
- أربط حساب GitHub واختر المستودع الذي أنشأته.
- اسم الخدمة (اختياري): `1wxxv` (قمنا بتحضير ملفات `render.yaml` و`scripts` لذلك).
- اختر الفرع `main` وادخل أو تأكد من إعدادات البناء:
    - Build Command: `bash build.sh`
    - Start Command: `bash start.sh`

3) متغيرات البيئة مقترحة

- `SECRET_KEY` — قيمة عشوائية للاستخدام في بيئة الإنتاج.
- إذا استخدمت قاعدة بيانات خارجية: `DATABASE_URL` (URI).

ملاحظات:
- لدينا بالفعل `render.yaml`, `build.sh`, `start.sh` في المستودع لتسهيل النشر. إذا أردت، يمكن استخدام واجهة Render أو تشغيل `render` CLI/Infrastructure as Code.
- تأكد من عدم رفع ملفات حساسة مثل مفاتيح سرية أو قاعدة `ecommerce.db` إلى المستودع العام.

إذا تريد، أستطيع إنشاء ملف `.gitignore` وأقدم سكربت PowerShell جاهزًا لتشغيل الأوامر أعلاه.
