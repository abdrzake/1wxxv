# نشر التطبيق على Heroku - خطوات سريعة 🚀

## المتطلبات:
- حساب على [Heroku](https://www.heroku.com)
- Git مثبت على الكمبيوتر
- Heroku CLI مثبت

## الخطوات:

### 1️⃣ تحضيري (مرة واحدة فقط)

```bash
# فتح CMD أو PowerShell في مجلد المشروع
cd c:\Users\abdo\python261

# تثبيت Heroku CLI من:
# https://devcenter.heroku.com/articles/heroku-cli

# التحقق من التثبيت
heroku --version

# تسجيل الدخول
heroku login
```

### 2️⃣ إعداد Git

```bash
# إنشاء repository جديد
git init

# إضافة جميع الملفات
git add .

# أول commit
git commit -m "Initial commit - E-commerce store"
```

### 3️⃣ إنشاء التطبيق على Heroku

```bash
# إنشاء تطبيق جديد
heroku create 1wxxv

# مثال:
heroku create 1wxxv

# أو اترك Heroku يختار الاسم
heroku create
```

### 4️⃣ النشر

```bash
# رفع الملفات إلى Heroku
git push heroku main

# أو إذا كان الفرع الرئيسي master:
git push heroku master
```

### 5️⃣ فتح الموقع

```bash
# فتح التطبيق في المتصفح
heroku open

# أو نسخ الرابط
heroku info
```

## الرابط النهائي سيكون شيء مثل:
```
https://your-app-name.herokuapp.com
```

---

## بدائل أخرى (أسهل للمبتدئين):

### ✅ استخدام PythonAnywhere (موصى به):

1. اذهب إلى [PythonAnywhere](https://www.pythonanywhere.com)
2. انشئ حساب مجاني
3. قم برفع ملفات المشروع
4. إنشاء Web App
5. الرابط: `https://your-username.pythonanywhere.com`

### ✅ استخدام Render:

1. اذهب إلى [Render](https://render.com)
2. ربط حساب GitHub
3. اختر المشروع وانشر

---

## اختبر قبل النشر:

```bash
# تثبيت gunicorn محلياً
pip install gunicorn

# تشغيل التطبيق
gunicorn app:app

# سيكون متاح على: http://localhost:8000
```

---

## مراقبة التطبيق بعد النشر:

```bash
# عرض السجلات
heroku logs --tail

# إعادة تشغيل
heroku restart

# عرض معلومات التطبيق
heroku info
```

---

## في حالة الأخطاء:

```bash
# عرض الأخطاء بالتفصيل
heroku logs --tail

# إعادة بناء التطبيق
heroku rebuild

# حذف التطبيق (إذا أردت ابدء من جديد)
heroku apps:destroy --app your-app-name
```

---

## تحديث التطبيق بعد التعديلات:

```bash
# بعد أي تعديل على الملفات:
git add .
git commit -m "وصف التعديلات"
git push heroku main
```

---

**اسأل إذا احتجت مساعدة! 💪**
